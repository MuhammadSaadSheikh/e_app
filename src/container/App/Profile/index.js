import React from 'react';
import {ScrollView, View} from 'react-native';

//GlobalStyle
import {globalStyle} from '../../../theme';

const Profile = () => {
  return <ScrollView style={globalStyle.mainContainer}></ScrollView>;
};

export default Profile;
